# Production - use in Vercel Environment Variables
NEXT_PUBLIC_API_URL=https://callflowbackend.onrender.com
# JWT_SECRET - same as backend (for middleware JWT validation). Server-side only.
# JWT_SECRET=your-secret-key
